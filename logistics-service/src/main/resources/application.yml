server:
  port: 8083

spring:
  application:
    name: logistics-service

temporal:
  service-url: 127.0.0.1:7233
  namespace: default

logging:
  level:
    com.example.temporal: DEBUG
    io.temporal: INFO

